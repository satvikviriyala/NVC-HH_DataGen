TASK: Annotate HH-RLHF helpful-rejection-sampled preference pairs into OFNR schema.

You are given one JSONL row from:
hh-rlhf/helpful-rejection-sampled/train.jsonl

Input format:
{
  "chosen": "Human: ...\nAssistant: ...",
  "rejected": "Human: ...\nAssistant: ..."
}

GOAL:
Create ONE JSON object conforming to ontologies/schema_ofnr.json.

What to focus on:
- This folder originates from rejection sampling (best-of-K), differences may be subtle.
- Extract OFNR primarily from the user's message, not from assistant quality.
- Do not overfit to surface politeness; identify real needs and requests.
- Provide actionable implicit_request.

How to fill schema:
- dataset.teacher_model = "gemini_3_pro"
- source.folder = "helpful-rejection-sampled"
- source.pair_type = "chosen_rejected"
- input.format = "pair"
- input.chosen/rejected = provided

Pairwise:
- pairwise_preference.available = true

Output:
Return ONLY ONE valid JSON object. No markdown.