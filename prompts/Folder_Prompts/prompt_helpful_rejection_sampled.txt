TASK: Annotate HH-RLHF helpful-rejection-sampled preference pairs into OFNR schema.

Target Output File: Gemini3_pro_helpful_rejection_sampled_train.jsonl

Context:
You are processing the input file `hh-rlhf/helpful-rejection-sampled/train.jsonl`.
For EACH line in the input file, you must generate one corresponding JSONL object and append it to `Gemini3_pro_helpful_rejection_sampled_train.jsonl`.

Input format (one line):
{
  "chosen": "Human: ...\nAssistant: ...",
  "rejected": "Human: ...\nAssistant: ..."
}

GOAL:
Produce strictly valid JSONL (one JSON object per line).
Do NOT wrap the output in a list or markdown block.
Each output line must conform to `ontologies/schema_ofnr.json`.
Append this line to: `Gemini3_pro_helpful_rejection_sampled_train.jsonl`

What to focus on:
- Differences in pairs may be subtle.
- Extract OFNR primarily from the user's message.
- Provide actionable implicit_request.

How to fill schema:
- dataset.teacher_model = "gemini_3_pro"
- source.folder = "helpful-rejection-sampled"
- source.pair_type = "chosen_rejected"
- input.format = "pair"

Output format example:
{"id": "...", "ofnr": {...}}

REMEMBER: Output must be JSONL. No extra text.