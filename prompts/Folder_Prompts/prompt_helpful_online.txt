TASK: Annotate HH-RLHF helpful-online preference pairs into OFNR schema.

You are given one JSONL row from:
hh-rlhf/helpful-online/train.jsonl

Input format:
{
  "chosen": "Human: ...\nAssistant: ...",
  "rejected": "Human: ...\nAssistant: ..."
}

GOAL:
Create ONE JSON object conforming to ontologies/schema_ofnr.json.

What to focus on:
- These pairs come from iterative online collection; responses can be more varied.
- Extract stable needs even if conversation is chaotic.
- Apply judgment marker filter for observation.
- Use PLATO filter strictly.

How to fill schema:
- dataset.teacher_model = "gemini_3_pro"
- source.folder = "helpful-online"
- source.split = "train" (unless otherwise)
- source.pair_type = "chosen_rejected"
- input.format = "pair"

Pairwise:
- pairwise_preference.available = true

Safety:
- If disallowed, annotate safety and provide safe alternative.

Output:
Return ONLY ONE valid JSON object. No markdown.